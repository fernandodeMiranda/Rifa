RewriteEngine On

# Serve arquivos e diretórios existentes diretamente (assets, uploads).
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Tudo o mais passa pelo front controller.
RewriteRule ^ index.php [L]
