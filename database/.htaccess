# Bloqueia acesso web direto a esta pasta (contém schema.sql/seed.sql).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
